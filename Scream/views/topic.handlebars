<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="../posts.css" rel="stylesheet">
    <title>E-Scream!</title>

    <style>
        h1{
            border-radius:10%/10%;
            border: 5px white double;
        }
       #content{
            margin-top: 30px;
            background-color:cadetblue;
            color: azure;
            font-family: "Marker Felt", "Comic Sans MS", fantasy;
            font-size: 1.2em;
            padding: 30px;
            border-radius:5%/10%;
            border: 5px white double;
       }
        .main {
            width:70%;
        }

        .each-reply {
            width: 100%;
            text-align: left;
        }

        .reply-name {
            height: 100%;
            float: left;
            color: white;
            background-color: #83b582;
            font-family: "Marker Felt", "Comic Sans MS", fantasy;
            font-size: 1.2em;
            padding: 2px 5px;
            border-radius: 10px;
        }

        .reply-content {
            height: 100%;
            display: inline-block;
            margin: 2px 20px;
            font-family: "Marker Felt", "Comic Sans MS", fantasy;
        }

        .reply-time {
            /* border: 2px green solid; */
            height: 100%;
            display: inline-block;
            margin: -2px 20px;
        }

        .comment-box {
            margin: 25px auto;
        }

        .comment-form {
            margin: 30px auto;
        }

        h2 {
            color: #004445;
            font-family: "Marker Felt", "Comic Sans MS", fantasy;
        }

        textarea {
            border-radius:5%/10%;
            width: 95%;
            height: 10em;
            overflow: auto;
            font-size: 15px;
            margin: auto;
            padding: 15px;
            border: 2px #fda77f dashed;
            background-color: #fff1cf;
        }

        #delete a,
        .reply-time {
            padding: 3px;
            color: #fda77f;
            font-family: "Marker Felt", "Comic Sans MS", fantasy;
        }

        li a,
        #delete a {
            text-decoration: none;
        }
    </style>
</head>

<body>

    <div class="main">
        <h1>{{post.title}}</h1>
        <div id='content'>
            {{post.content}}
        </div>
        <hr>

        <h2>Comments:</h2>
        <div class="comment-box">
            {{#each comments}}
            <div class="each-comment-box comment-box">
                <div class="each-reply">
                    <div class="reply-name">{{this.commenter}}</div>
                    <div class="reply-content">{{this.content}}</div>
                </div>

                <div class="controls">
                    <div class="reply-time">{{this.reply_time}}</div>
                    <div id="delete"><a href="/delete-comment/{{this.id}}">Delete</a></div>
                </div>
                <hr>
            </div>

            {{/each}}
        </div>
        <hr>

        <div class="comment-form">
            <form action="/submit/{{post.id}}" method="post">
                <h2>What do you think of it?</h2>
                <div>
                    <textarea name="content" cols="60" rows="10"></textarea>
                </div>
                <input type="submit" value="Reply">
            </form>
        </div>
    </div>
</body>

</html>